# Core dependencies (updated for RTX 5090 support)
torch>=2.5.0
transformers>=4.46.0
peft>=0.13.0
accelerate>=1.1.0
bitsandbytes>=0.44.0
safetensors>=0.4.5
datasets>=3.0.0

# Cryptography
pycryptodome>=3.19.0
cryptography>=41.0.0

# Compression
zstandard>=0.22.0

# RunPod
runpod>=1.7.0

# PDF Processing (for synthetic Q&A generation)
pypdf>=4.0.0

# Utilities
python-dotenv>=1.0.0
scipy>=1.11.0

# Fast HuggingFace downloads (required when HF_HUB_ENABLE_HF_TRANSFER=1)
hf_transfer>=0.1.0

# Optional: Training with Axolotl
# axolotl[flash-attn,deepspeed]>=0.4.0

# Optional: Monitoring
# wandb>=0.16.0
