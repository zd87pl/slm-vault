{
  "_comment": "Sample only — run `enclave mcp install` to generate and merge the real config (it fills in your venv's python path automatically).",
  "mcpServers": {
    "enclave": {
      "command": "python",
      "args": ["-m", "advanced_vault.mcp_server"],
      "env": {
        "VAULT_PATH": "~/.vault"
      }
    }
  }
}
